_work/
  Add unincorporated changes in here, such as kerl, exenv etc
  needs to alter the path, add extra commands in any dotfile
  after install that is temporary or specific to an distro.

copy/
  Everything that only needs to be copied to $HOME.

pebbles/
  Any dotfile and config directory that needs to be backed up
  and needs a symlink.

add/
  Config or supplemental files/dir that can be referenced
  from where they are.

install-scripts/
  Install scripts that are sourced from dot-install.sh
